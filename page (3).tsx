/** Customer care and policy page. */
'use client'; import {useState} from 'react'; const rows=[['Shipping','Free tracked shipping on orders over $75. Orders dispatch in 1–3 business days; delivery timing varies by destination.'],['Returns','Return unused items within 30 days of delivery. Contact care@daoyuan.field before sending an item back.'],['Privacy','We use your information only to process orders, provide customer support, and improve the store experience.'],['Contact','For order or product questions, email care@daoyuan.field. We reply Monday–Friday, within two business days.']];
export default function Care(){const[o,setO]=useState<number|null>(0);return <div className="mx-auto max-w-3xl px-5 py-16"><p className="text-[10px] uppercase tracking-[.25em] text-gold">Customer care</p><h1 className="display mt-4 text-6xl">A clear path back.</h1><p className="mt-5 text-sm leading-7 text-pearl/60">Practical information for your order, your objects, and your privacy.</p><div className="mt-12 border-t border-gold/20">{rows.map(([t,d],i)=><div key={t} className="border-b border-gold/20"><button onClick={()=>setO(o===i?null:i)} className="flex w-full items-center justify-between py-6 text-left"><span className="display text-3xl">{t}</span><span className="text-gold">{o===i?'−':'+'}</span></button>{o===i&&<p className="max-w-xl pb-6 text-sm leading-7 text-pearl/60">{d}</p>}</div>)}</div></div>}
