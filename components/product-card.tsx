/** Interactive catalog card with compliant product microcopy. */
'use client'; import Link from 'next/link'; import {motion} from 'framer-motion'; import {Product} from '@/lib/data'; import {ProductVisual} from './product-visual';
export function ProductCard({product}:{product:Product}){return <motion.article whileHover={{y:-5}} className="group border border-gold/15 bg-[#11110f] transition hover:border-gold hover:shadow-[0_0_24px_rgba(201,169,110,.14)]"><Link href={`/product/${product.slug}`}><div className="overflow-hidden"><motion.div whileHover={{scale:1.045}} transition={{duration:.5}}><ProductVisual color={product.color} name={product.name}/></motion.div></div></Link><div className="p-4"><p className="text-[9px] uppercase tracking-[.18em] text-gold">{product.energy} / {product.form}</p><Link href={`/product/${product.slug}`}><h3 className="display mt-2 text-2xl">{product.name}</h3></Link><p className="mt-2 h-10 text-xs leading-5 text-pearl/55">{product.description}</p><div className="mt-4 flex items-center justify-between"><span className="text-sm text-gold">${product.price}</span><button data-add className="text-[9px] uppercase tracking-[.14em] text-gold">Quick add +</button></div></div></motion.article>}
