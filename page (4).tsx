/** Filterable product collections page. */
'use client'; import {useState} from 'react'; import {motion} from 'framer-motion'; import {products} from '@/lib/data'; import {ProductCard} from '@/components/product-card';
export default function Collections(){const[tab,setTab]=useState<'energy'|'form'>('energy'),[filter,setFilter]=useState('All');const opts=tab==='energy'?['All','Connection','Grounding','Flow','Clarity']:['All','Wearable','Stationary','Carryable'];const visible=products.filter(p=>filter==='All'||(tab==='energy'?p.energy:p.form)===filter);return <motion.div initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} className="mx-auto max-w-[1200px] px-5 py-16"><p className="text-[10px] uppercase tracking-[.24em] text-gold">The collections</p><h1 className="display mt-3 text-5xl md:text-7xl">Align Your Frequency</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-pearl/60">Each crystal carries a unique Lattice Thread. Choose a form and a reminder for your 11:11 Quiet Hour.</p><div className="mt-12 flex border-b border-gold/20"><button onClick={()=>{setTab('energy');setFilter('All')}} className={`px-6 py-4 text-[10px] uppercase tracking-[.2em] ${tab==='energy'?'border-b border-gold text-gold':'text-pearl/45'}`}>By Energy</button><button onClick={()=>{setTab('form');setFilter('All')}} className={`px-6 py-4 text-[10px] uppercase tracking-[.2em] ${tab==='form'?'border-b border-gold text-gold':'text-pearl/45'}`}>By Form</button></div><div className="my-7 flex flex-wrap gap-2">{opts.map(x=><button key={x} onClick={()=>setFilter(x)} className={`rounded-full border px-4 py-2 text-[10px] uppercase tracking-wider ${filter===x?'border-gold bg-gold text-ink':'border-gold/25 text-pearl/60'}`}>{x}</button>)}</div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{visible.map(p=><ProductCard key={p.slug} product={p}/>)}</div></motion.div>}
