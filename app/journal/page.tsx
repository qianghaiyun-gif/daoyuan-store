/** Energy Journal listing and detail-style reader. */
'use client'; import {useState} from 'react'; import {motion} from 'framer-motion';
const stories=[['The 11:11 Guide','A small guide to making a gentler pause in the day.'],['Crystal as Object','A note on material, craft, and symbolic tradition.'],['The Quiet Hour','A ritual can be nothing more than attention, repeated.'],['A Practice of Placement','Why the objects in a room can change how we notice it.']];
export default function Journal(){const[open,setOpen]=useState<number|null>(null);return <div className="mx-auto max-w-[1200px] px-5 py-16"><p className="text-[10px] uppercase tracking-[.25em] text-gold">Energy Journal</p><h1 className="display mt-4 text-6xl">Notes from the field.</h1><div className="mt-12 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{stories.map(([t,d],i)=><motion.button whileHover={{y:-5}} key={t} onClick={()=>setOpen(i)} className="min-h-72 border border-gold/20 bg-[#12110f] p-6 text-left"><span className="text-[10px] tracking-widest text-gold">ESSAY 0{i+1}</span><h2 className="display mt-24 text-4xl">{t}</h2><p className="mt-3 text-xs leading-5 text-pearl/55">{d}</p></motion.button>)}</div>{open!==null&&<article className="mx-auto mt-16 max-w-2xl border-t border-gold/25 pt-12"><button onClick={()=>setOpen(null)} className="text-xs text-gold">← All notes</button><h2 className="display mt-5 text-6xl">{stories[open][0]}</h2><p className="mt-7 text-sm leading-8 text-pearl/65">{stories[open][1]} This journal is an invitation to examine the objects, rhythms, and images that hold your attention. There is no promised outcome here: only a place to return to a chosen thought.</p></article>}</div>}
